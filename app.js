// A220 Pro - interfaz y arranque
function showView(id,btn){document.querySelectorAll('.view').forEach(v=>v.classList.remove('active'));document.getElementById(id)?.classList.add('active');document.querySelectorAll('nav button').forEach(b=>b.classList.remove('active'));if(btn)btn.classList.add('active');renderAll()} function renderDashboard(){if(!appData)return;const today=new Date().toLocaleDateString('es-AR'),sales=appData.sales.filter(s=>new Date(s.date).toLocaleDateString('es-AR')===today),total=sales.reduce((a,s)=>a+s.total,0),units=sales.reduce((a,s)=>a+s.items.reduce((b,i)=>b+i.qty,0),0);document.getElementById('dVentas').textContent=money(total);document.getElementById('dProductos').textContent=appData.products.length;document.getElementById('dBajo').textContent=appData.products.filter(p=>p.stock<=p.minStock).length;document.getElementById('dUnidades').textContent=units;const low=appData.products.filter(p=>p.stock<=p.minStock);document.getElementById('alertas').innerHTML=low.length?low.map(p=>`<div>⚠️ ${escapeHTML(p.name)}: ${p.stock}</div>`).join(''):'Sin alertas.'} function renderAll(){if(!isLoggedIn||!appData)return;renderDashboard();renderProducts();renderMoves();renderCart()} function toggleTheme(){const dark=document.documentElement.dataset.theme!=='dark';document.documentElement.dataset.theme=dark?'dark':'light';localStorage.setItem(APP_CONFIG.themeKey,dark?'dark':'light')} function boot(){const theme=localStorage.getItem(APP_CONFIG.themeKey)||'light';document.documentElement.dataset.theme=theme;loadGitHubConfig();document.getElementById('loginPass').addEventListener('keydown',e=>{if(e.key==='Enter')doLogin()});document.getElementById('buscarProducto').addEventListener('input',filterSaleProducts);document.getElementById('saleProduct').addEventListener('change',updateSalePreview);document.getElementById('salePrice').addEventListener('input',updateSalePreview);document.getElementById('productFilter').addEventListener('input',renderProducts);bootAuth()} document.addEventListener('DOMContentLoaded',boot);