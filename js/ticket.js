// A220 Pro - ticket térmico 58 mm mediante impresión del navegador
function printLastSale(){const s=appData&&appData.sales&&appData.sales[0];if(!s){showToast('No hay una venta para imprimir','error');return}const old=document.getElementById('a220PrintStyle');if(old)old.remove();const st=document.createElement('style');st.id='a220PrintStyle';st.textContent='@page{size:58mm auto;margin:2mm}@media print{body>*:not(#a220PrintTicket){display:none!important}#a220PrintTicket{display:block!important;width:54mm;font:11px Arial,sans-serif;color:#000}#a220PrintTicket h1{text-align:center;font-size:18px;margin:0 0 4px}#a220PrintTicket .right{text-align:right}}#a220PrintTicket{display:none}';document.head.appendChild(st);const rows=(s.items||[]).map(i=>escapeHTML(i.name)+'<br>'+i.qty+' x '+money(i.price)+' = '+money(i.total)).join('<hr>');const box=document.createElement('div');box.id='a220PrintTicket';box.innerHTML='<h1>A220</h1><div style="text-align:center">Kiosco</div><div style="text-align:center">'+escapeHTML(new Date(s.date).toLocaleString('es-AR'))+'</div><hr><div>'+rows+'</div><hr><div class="right"><strong>TOTAL '+money(s.total)+'</strong></div><div>Medio de pago: efectivo</div>'+(Number(s.paid)>0?'<div>Recibido: '+money(s.paid)+'</div><div>Vuelto: '+money(s.change)+'</div>':'')+'<div style="text-align:center;margin-top:10px">¡Gracias por tu compra!</div>';document.body.appendChild(box);window.print();setTimeout(()=>{box.remove();st.remove()},800)}
function getA220PurchaseItems(){if(!appData||!Array.isArray(appData.products))return[];return appData.products.filter(p=>Number(p.stock||0)<=Number(p.minStock||0)).map(p=>{const stock=Number(p.stock||0),min=Number(p.minStock||0);const qty=Math.max(1,min-stock);return{p,stock,min,qty,cost:Number(p.cost||0),total:qty*Number(p.cost||0)}})}
function printPurchaseList(){const items=getA220PurchaseItems();if(!items.length){showToast('No hay productos para reponer','info');return}const old=document.getElementById('a220PurchasePrintStyle');if(old)old.remove();const st=document.createElement('style');st.id='a220PurchasePrintStyle';st.textContent='@page{size:58mm auto;margin:2mm}@media print{body>*:not(#a220PurchaseTicket){display:none!important}#a220PurchaseTicket{display:block!important;width:54mm;font:11px Arial,sans-serif;color:#000}#a220PurchaseTicket h1{text-align:center;font-size:17px;margin:0 0 4px}#a220PurchaseTicket .right{text-align:right}#a220PurchaseTicket hr{border:0;border-top:1px dashed #000;margin:5px 0}}#a220PurchaseTicket{display:none}';document.head.appendChild(st);const rows=items.map(x=>'<div><strong>'+escapeHTML(x.p.name||'Producto')+'</strong><br>Stock: '+x.stock+' / mínimo: '+x.min+'<br><strong>COMPRAR: '+x.qty+'</strong>'+(x.cost>0?'<br>Costo est.: '+money(x.total):'')+'</div>').join('<hr>');const total=items.reduce((n,x)=>n+x.total,0);const box=document.createElement('div');box.id='a220PurchaseTicket';box.innerHTML='<h1>LISTA DE COMPRA</h1><div style="text-align:center">A220 · '+escapeHTML(new Date().toLocaleString('es-AR'))+'</div><hr>'+rows+'<hr><div class="right"><strong>PRODUCTOS: '+items.length+'</strong><br>COSTO EST.: '+money(total)+'</div><div style="text-align:center;margin-top:10px">Reponer según stock mínimo</div>';document.body.appendChild(box);window.print();setTimeout(()=>{box.remove();st.remove()},800)}
function ensureA220PurchaseButton(){const alerts=document.getElementById('alertas');if(!alerts||document.getElementById('a220PurchaseButton'))return;const b=document.createElement('button');b.id='a220PurchaseButton';b.className='btn btn-outline btn-block';b.type='button';b.textContent='🛒 Imprimir lista de compra';b.onclick=printPurchaseList;alerts.insertAdjacentElement('afterend',b)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',ensureA220PurchaseButton);else ensureA220PurchaseButton();