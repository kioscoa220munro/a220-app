// A220 Pro - núcleo de datos
let appData=null;
const EMPTY_DATA=()=>({schemaVersion:4,updatedAt:new Date().toISOString(),revision:0,products:[],sales:[],moves:[],scans:[]});
function normalizeData(raw){const d=raw&&typeof raw==='object'?raw:{},out=EMPTY_DATA();out.schemaVersion=Number(d.schemaVersion)||4;out.updatedAt=d.updatedAt||new Date().toISOString();out.revision=Number(d.revision)||0;out.products=Array.isArray(d.products)?d.products:[];out.sales=Array.isArray(d.sales)?d.sales:[];out.moves=Array.isArray(d.moves)?d.moves:[];out.scans=Array.isArray(d.scans)?d.scans:[];out.products=out.products.map((p,i)=>({id:Number(p.id)||i+1,name:String(p.name||'').trim(),cat:String(p.cat||'General').trim(),cost:Number(p.cost)||0,price:Number(p.price)||0,stock:Math.max(0,Number(p.stock)||0),minStock:Math.max(0,Number(p.minStock)||0),barcode:String(p.barcode||'').trim(),image:String(p.image||''),brand:String(p.brand||''),quantity:String(p.quantity||''),source:String(p.source||'A220').trim(),sourceUrl:String(p.sourceUrl||''),priceOrigin:String(p.priceOrigin||'A220').trim()})).filter(p=>p.name);return out;}
function getData(){return appData;} function nextId(items){return items.reduce((m,x)=>Math.max(m,Number(x.id)||0),0)+1;} function money(n){return '$'+Number(n||0).toLocaleString('es-AR',{maximumFractionDigits:2});} function dateTimeStr(){return new Date().toLocaleString('es-AR');} function escapeHTML(v){return String(v??'').replace(/[&<>'\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','\"':'&quot;','\\':'&#92;'}[c]||c));} function showToast(m,type='info'){const r=document.getElementById('toastContainer');if(!r)return;const e=document.createElement('div');e.className=`toast ${type}`;e.textContent=m;r.appendChild(e);setTimeout(()=>e.remove(),3200);} function downloadText(filename,text,mime='text/plain'){const b=new Blob([text],{type:mime}),u=URL.createObjectURL(b),a=document.createElement('a');a.href=u;a.download=filename;document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(u),1000);}
